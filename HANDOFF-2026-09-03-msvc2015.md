# Handoff — MSVC 2015 plug-ins, and where their editors stop

**Status: both editors draw in full. The VST3 loads, renders and paints its
whole interface; the i386 VST2 loads with 110 parameters and paints its whole
interface too, through the bridge as well as directly.**

Two earlier readings in this document were wrong, and both are worth keeping
for the shape of the error. "The reasons are not more host work" was wrong
twice over: the i386 build was blocked by six absent pieces of Win32 rather
than by its download, and the VST3's blank editor was four missing pieces of
Direct2D and a region call that answered zero. Neither was subtle once
something asked the plug-in what it wanted instead of reasoning about what it
ought to want.

| plug-in | result |
|---|---|
| `MinimogueVA.vst3` (x86-64) | **renders audio** — peak 0.2512 over 2 s; 234 parameters, correctly named. **Editor draws completely**: the cabinet, the panel, every knob with its tick marks, every label, the arpeggiator strip and the logo, at 1004x590 |
| `MinimogueVA.dll` (i386, VST2) | **loads, 110 parameters, renders at peak 0.5199, editor draws in full** — cabinet, panel, every knob and label, at 1002x589, through the bridge as well as directly. Its SynthEdit modules all load; the "Can't load Module" failures are gone |

This started as "why will these two not open" and turned into the MSVC 2015
runtime, which nothing in the existing corpus uses: 107 of the 115 Windows
plug-ins here are statically linked, and the eight that are not are the Native
Instruments four at both widths, which use the 2013 runtime that
`HANDOFF-2026-08-01-msvcp120.md` is about.

Regression, re-run after every change below, by rendering rather than only
loading. The corpus is not in this tree; on this machine it is under
`/storage01/synth_stuff/vst/windows/{VST2-64,VST3,VST2-32}`.

    VST2-64  39/40 load, 36 make sound   (NI Massive calls ExitProcess(1))
    VST3     35/35 load, 33 make sound   (brokenmini and drumtraqs were
                                          already silent, verified against
                                          the previous commit each time)
    VST2-32  36/40 load, 34 make sound   (the NI four want runtime32/)

Re-run in full after the editor work below, and unchanged. Editors were
measured the same way, which had not been done before:

    VST2-64 + VST3   74 of 75 editors open and paint, none blank
                     (NI Massive is the one, and it never loads)

That is a stricter test than "opens its own GUI", which is what the numbers in
this document used to mean: it captures the window and counts the pixels that
are not black. Every plug-in in the corpus that opens an editor now draws
something into it.

    S=/tmp; for f in /storage01/synth_stuff/vst/windows/VST2-64/*; do
      ./peload/build/peload "$f" --editor $S/e.ppm 2>&1 | grep -oE 'captured.*'
    done

## The one mistake, made eight times

Every blocker in this document is the same shape: **a stub returning 0, where 0
is the API's word for success.**

  * GDI+ `Status`: 0 is `Ok`
  * `HRESULT`: 0 is `S_OK`
  * `_initterm_e`: 0 is "every initialiser succeeded"
  * `scanf`: a count of 0 assignments is a legitimate answer
  * `GetModuleHandleEx`, `FindFirstChangeNotification`: 0 is failure here,
    which the caller reads as a reason to give up rather than as "nothing"

The generic stub in `missing_import()` returns 0 because that is the safe answer
for a `void` or a `BOOL`. For a status code it is the opposite of safe: it tells
the plug-in the call worked, so the plug-in takes the pointer it was never
given and faults somewhere else entirely, a long way from the stub. Every
diagnosis below was slow for that reason, and `stub_failure_value()` is where
the fix belongs — it now covers `gdiplus` (GenericError) and `d3d*`/`d2d*`/
`dxgi` (E_FAIL) as families rather than function by function.

**If a plug-in dies far from anything you changed, look for a stub whose zero
means yes.**

## The 2015 C runtime arrives under fifteen other names

`crt_alias()` mapped `msvcr*`, `msvcp*`, `vcruntime*` and `ucrtbase` onto the
one CRT implementation here. MSVC 2015 split the CRT into apiset DLLs —
`api-ms-win-crt-runtime-l1-1-0`, `-stdio-`, `-heap-`, `-convert-` and a dozen
more, each forwarding to ucrtbase — and none of them matched, so a 2015-built
plug-in imported its **entire** C runtime under names that fell through to the
generic stub.

`_initterm_e` is what made it fatal rather than merely wrong. It walks an array
of initialiser pointers and calls each, which is how a C++ module runs its
static constructors; stubbed, it reported success having run none. A
side-loaded MSVCP140 mapped, said it had initialised, and left every locale and
iostream global null. **With the alias added it runs 554 constructors instead
of none.** The implementation was already here under `msvcrt.dll`; only the
name was missing.

Matching is by prefix, because the apiset names carry a version suffix that
changes between Windows releases and some import tables truncate them.

## scanf did not exist

There was no `sscanf` in this host at all, so the UCRT's
`__stdio_common_vsscanf` fell to the stub. A SynthEdit plug-in parses its whole
module graph out of XML with it: every module id came back unset, every registry
lookup missed, and the plug-in crashed reading address `0x3c8` — which is
`lea rcx,[rax+0x3c8]` on a null `rax` two instructions after the lookup, in a
function whose *next* block tests the same pointer before using it.

Written out rather than delegated, for the same reason `w32_vfmt` is: on x86-64
the guest passes Microsoft's `va_list` and the host's `vsscanf` expects System
V's, so every argument pointer would be read from the wrong register or slot.

**Windows `long` is 32 bits.** `%ld` stores four bytes here, not eight. Storing
eight scribbles on whatever the guest put beside the variable, silently.

With scanf the plug-in loads: 235 parameters. The wide CRT beside it —
`wcslen` was the only `wcs*` present — is what makes those parameters *named*.

## dynamic_cast

`__RTDynamicCast` was a generic stub. A stub returning 0 is not a soft failure:
the compiler emits a null check only where the source admits the cast can fail,
so a cast the programmer knows cannot fail is used immediately.

Found by disassembly, not guessed. The faulting call target was an import
thunk; its IAT slot at `0x19d988` resolved to `VCRUNTIME140.dll!__RTDynamicCast`.

The implementation walks MSVC's own RTTI: the vftable is preceded by a
`CompleteObjectLocator` naming the type, saying where the subobject sits inside
the complete object, and pointing at a hierarchy descriptor listing every base.
Two details matter:

  * **Compare mangled names, not TypeDescriptor addresses.** A class defined in
    two images has two descriptors. MSVC's own runtime compares names for this
    reason.
  * The locator carries its own RVA, which is how an object works out where its
    RTTI lives without the host knowing which module it came from.

Instrumented over a load it does 15,384 successful casts. The failures — 4,656
to `ug_container`, 2,804 to `ug_patch_automator` — are ordinary type tests, and
are not logged for that reason.

## A plug-in could not find out where it was

`GetModuleFileName` answered with a fixed `C:\peload\plugin.dll` whatever was
loaded. Enough for a plug-in that wants *a* name; wrong for the very common one
that takes the directory out of it and loads its own data from beside itself —
`path_norm_n` maps that to `/peload/`, which holds nothing. SynthEdit asks for
its `.sem` modules that way and got seven nulls in a row.

The real path goes back now, separators flipped and a drive letter in front, so
a guest splitting on a backslash finds the right pieces and `path_norm_n` turns
whatever it rebuilds back into the host path it came from. Set for the plug-in
only, and **saved across a runtime side-load** — an msvcp140 mapped out of
`runtime/` must not become the answer to "where am I".

## LoadLibrary could not load a library

It only ever answered for the system DLLs this host shims. Everything else got
NULL — including a file sitting right there that this program, a PE loader,
knows exactly how to map. A plug-in shipping part of itself as separate DLLs got
nothing.

`winstubs.h` has no loader of its own and is shared by two that differ in width,
so each installs a pair of hooks (`winstubs_set_loader`) and LoadLibrary reaches
the real thing through them. A wrong-width DLL simply fails to map, which is the
right answer rather than a special case. Modules are kept in a table so one
asked for repeatedly is mapped once and keeps the same handle.

`GetProcAddress` asks a real module for its own exports **first** — the module
really does define those names and a same-named stub would shadow the genuine
one — and **falls through on a miss** rather than returning. An early return
there broke every `GetProcAddress` on a handle that is not ours, which had been
resolving through the name search below it.

`LoadLibraryExA` was missing while the W form was present. That is what the
delay-load helper reaches for; without it the first delay-loaded import raised
`0xC06D007E`.

## What the plug-in told us once its stdio worked

Implementing `__acrt_iob_func` and `__stdio_common_vfprintf` was worth more than
the calls themselves. The plug-in immediately printed:

    ERROR: Unexpected NULL from FindFirstChangeNotification.

It watches its own folder so it can pick up a skin dropped in beside it, and
read the null as a failure of the host rather than as "no changes" — then
abandoned building its DSP graph, which rendered silence with no other symptom.
An unsignalled event is the right object: a watcher polling it times out, which
is what it expects.

`WaitForMultipleObjects` came with it, and a stub there is worse than a missing
one: it returns 0, which is `WAIT_OBJECT_0`, so the watcher is told its files
changed on every pass.

**Give a plug-in somewhere to print. It will tell you what is wrong.**

## New subsystems

Three, all under `#ifndef PELOAD_NO_GUI_LAYER`, all built on machinery that was
already here:

  * **WIC** (`dwrite_shim.h`) — `CoCreateInstance` was not implemented at all,
    so a plug-in asking COM for an `IWICImagingFactory` got nothing and left the
    pointer null in a structure it then used. Decoding only: name a file, get its
    pixels, through the `png_in.h` readers written for the macOS side's ImageIO.
    MinimogueVA decodes all 96 of its images through it.
  * **DirectWrite's other half** (`dwrite_shim.h`) — this shim was written for
    Skia, which lays text out itself. `GetSystemFontCollection` handed back a
    bare probe, because Skia registers a collection of its own and never asks for
    the system one. Added the collection, family, font, face-name and
    localised-string objects, `GetGdiInterop`, `CreateTextFormat` and
    `CreateTextLayout`.
  * **GDI+** (`gdiplus_shim.h`, new, ~1170 lines) — the 96 entry points this
    corpus imports. A DC's pixels are a `w32_surf`, glyphs come from the same
    FreeType face DirectWrite measures with, images decode through `png_in.h`;
    the one piece genuinely missing was a rasteriser, which flattens curves to
    segments and fills by scanline under either winding rule, sampling four
    times vertically.

`GetDeviceCaps` deserves its own line. A stub answering 0 is not neutral:
`LOGPIXELSX` is the display DPI and a plug-in scales its whole layout by
`dpi/96`, so zero collapsed the interface to nothing. **That alone was the 0x0
editor.**

## Where each editor stops

### The VST3: Direct3D 11 — walked, not emulated

The first reading of this was wrong and is worth recording, because the
correction is the useful part.

Chased to the end rather than assumed:

  * The editor's device-creation function (`FUN_1800d1c20`, RVA `0xd1c20`) calls
    `D3D11CreateDevice` and then dereferences the device **without checking**.
  * Its unwind info is `UNW_FLAG_UHANDLER` only — that runs during unwinding, it
    does not catch. Both call sites (`0xd2660`, `0xd4690`) have no `EHANDLER`
    either. **There is no `__try` anywhere in the chain**, so implementing SEH
    dispatch would let it unwind and die rather than recover.
  * Its paint loop checks `DXGI_STATUS_OCCLUDED`, which is a swap-chain present
    loop. No renderer selection to steer, no version check to spoof — the
    `GetVersionExW` call in that function is a feature check *inside* the D3D
    path, not a choice between paths.

It imports four renderers (`d3d11`, `d2d1`, `gdiplus`, `opengl32`) and commits
to the first.

**What that first reading missed is that the plug-in only walks *through* D3D to
reach Direct2D, and Direct2D's drawing is ordinary 2D.** Nothing has to touch
hardware. `d3d_shim.h` provides the walk and nothing more: a device, an adapter,
a DXGI factory, a swap chain whose back buffer is the window's own pixels, and
the query-interface chain between them. Direct2D is where the work would happen,
and it renders into the same `w32_surf` a GDI+ graphics object writes to.

The whole chain runs:

    D3D11CreateDevice -> IDXGIDevice -> GetAdapter -> GetParent(IDXGIFactory2)
      -> CreateSwapChainForHwnd -> ID2D1Factory1::CreateDevice
      -> CreateDeviceContext -> GetBuffer -> CreateBitmapFromDxgiSurface
      -> SetTarget

**The editor opens and captures at 1004x590 where before it took the plug-in
down.** It draws nothing yet — see below.

Three traps on the way, each worth its own line:

  * **Probe vtables were 64 slots.** `ID3D11DeviceContext` has 108 methods and
    `ID2D1DeviceContext` is not far behind, so a call past the end landed on
    whatever followed the array. That presents as a jump to a null address with
    nothing at all to say why. 160 now.
  * **QueryInterface returned the same object for every interface**, so slot
    numbers from four vtables collided in the log and were worse than useless.
    Each interface is its own object now, and an unrecognised GUID is printed in
    full rather than guessed at from a slot number.
  * **The swap chain's HWND was ignored.** A plug-in nests its drawing window
    inside the one it was given — VSTGUI inside the container, the GMPI surface
    inside that — and it is the innermost that Direct2D targets. Drawing into the
    display window put the pixels where nothing was reading.

### Where it stopped, and why it does not any more

**Resolved.** The section below was written when the editor drew nothing, and
its reading -- that GMPI never marks its controls dirty -- was wrong in an
instructive way. The dirty list is not filled by GMPI at all. The paint handler
builds it from `GetUpdateRgn` and `GetRegionData`, and this host's
`GetRegionData` returned 0:

    n = GetRegionData(hrgn, 0, NULL);
    if (n != 0) { ... add each rect to the list ... }

The stub's comment supposed a caller would fall back to `GetRgnBox`. This one
does not; zero means the region is empty, so the list stayed empty and the
handler returned without calling a single Direct2D method -- which is exactly
what the note below observed, and attributed to the plug-in.

Three more, once it was drawing: `SetTransform` discarded the matrix, so the
whole interface drew at the origin on top of itself; `GetTransform` did not
exist and is read 298 times a paint; and there were no bitmaps at all, which is
most of what a skin is. `IWICImagingFactory::CreateBitmap` is where the panel
every other control sits on gets composed.

The original note follows, because the way it was wrong is the useful part.

### Where it stopped (superseded)

The plug-in's paint handler (`FUN_1800d2660`, the `GMPIGUI` window procedure's
message handler, RVA `0xd2660`) gates its drawing on two things:

    if (self[0xf] != 0 && self[10] != self[0xb]) { ... draw ... }

The second is a begin/end pair over its own **dirty rectangle list**. With that
list empty it returns without calling a single Direct2D method — not one, so
there is not even an `E_NOTIMPL` to follow. `WM_PAINT` is delivered and
`ValidateRect` is called at the top of the handler, so the message plumbing is
working; what is missing is whatever makes GMPI mark its own controls dirty.

The next person should start there: find what adds to that list (a layout pass,
a parameter notification, a resize) and what this host is not doing that would
trigger it. Everything downstream of it is already in place.

**The GDI+ work does not open this editor.** It is still right: the surface is
real, other plug-ins use it, and chasing it is what found the
`D3D11CreateDevice` `S_OK`.

### The i386 VST2: six missing pieces of Win32, not the download

Its imports are `GDI32`/`USER32`/`COMCTL32` — a classic GDI SynthEdit editor
built on MFC. **It loads now, with 110 parameters, and its editor opens at
1002x589.** The earlier reading — blocked by the download rather than the host —
was wrong, and worth recording because of how it was wrong: the missing modules
are real, and the plug-in survives them. Every actual blocker was a stub.

The chain, each step found by following the failure to the stub that caused it:

  1. **No i386 exception dispatch.** A throw reached `RaiseException` and
     aborted. Now walked; see the i386 half of `mscxxeh.h`.
  2. **The thrown type, once anything could report it:** `CMemoryException`,
     which is MFC's answer to a failed allocation, not to a missing module.
  3. **`LocalReAlloc`** was stubbed, so NULL, so out of memory — on a 32-byte
     allocation. With the Local/Global family implemented the plug-in loads.
  4. **`SetWindowsHookEx`** returning NULL makes MFC throw the same exception
     from inside window creation, and the `HCBT_CREATEWND` that follows is how
     it attaches its `CWnd`.
  5. **`CreateDIBSection`** did not exist at either width, and MFC reads NULL
     from it as out of memory too.
  6. **The USER32 rectangle family**, none of which existed. A layout that
     offsets a rectangle and gets it back unmoved never converges: an endless
     `SelectObject` pair and a stack overflow 72 frames deep.
  7. **`RectVisible`** answering 0 tells a plug-in none of its interface is
     visible, and it draws none of it — zero drawing calls into a correctly
     built window.

It renders too, at peak 0.5199, and every one of its SynthEdit modules loads:
the run that produced this document reported seven "Can't load Module" failures
and there are now none. They were never a missing download -- a 2007 SynthEdit
build carries its standard modules inside the DLL -- they were the same host
gaps as everything else here, and they went with them.

The `.sem` files beside the 2025 VST3 are a separate thing and are still
x86-64 only; nothing needs them.

### Why the editor tab is safe to click

Both failures killed the plug-in — the hang (`GetWindowInfo` never reporting a
size, so the caller polled forever) took the helper down and the audio with it.

There was briefly a guard for this: `pehost_editor_kind()` declined to open an
editor when the image imported Direct3D or Direct2D. **It is gone, and it should
be.** What made the editor fatal was `D3D11CreateDevice` returning `S_OK` with
no device, and the plug-in dereferencing what it was told it had. The walk in
`d3d_shim.h` returns real objects at every step, so there is no null to follow
and nothing to decline: the editor opens, captures, and leaves audio alone.

Declining would now cost the working editor for no gain, which is why the check
was removed rather than repaired.

No plug-in in the corpus imports Direct3D, Direct2D or GDI+, so nothing there is
affected by the shim either way.

## Runtime DLLs

`runtime/` and `runtime32/` are gitignored and **must stay that way** -- they
are Microsoft's, not ours.

`runtime/` is no longer needed by anything in the corpus: `msvcp_shim.h` covers
every symbol the four NI plug-ins import from `msvcp120.dll`, and they render
byte-identical audio without it. Keep a copy if you want to diff against the
real thing -- `tools/msvcp_dis.py` and `--msvcp-selftest` both read it.

`runtime32/` **is** still needed, by those same four at 32-bit, because the
reimplementation is inside `#if defined(__x86_64__)`. Move it aside and all four
segfault; everything else in the corpus loads either way. Doing the i386 half is
what would close that, and `MSTHIS` is the convention it needs.

**Wine's copies do not work** and are not a substitute: they are compiled
against Wine's ntdll and fault in their own startup. `find_real_dll()` refuses
Wine's directories for that reason. Telling them apart: Wine's carry a "Wine
builtin" marker and are roughly double the size -- msvcp120 is 660 KB from
Microsoft and 1.2 MB from Wine. Most copies on a machine with Proton installed
are Wine's; a game that installed the real redistributable leaves genuine ones
in its prefix's `system32`/`syswow64`.

## Reproducing

    # loads, renders, 234 named parameters
    ./peload/build/peload "…/MinimogueVA.vst3" --render out.wav --secs 2 --note 60

    # declines cleanly, audio unaffected
    ./peload/build/peload "…/MinimogueVA.vst3" --editor shot.ppm

    PELOAD_VERBOSE=1 …          # every stub on first call, [dwrite]/[wic] traces

Locating a crash without symbols, which is how most of this was found:

    gdb -batch -nx -ex run -ex "printf \"%p\n\", $rip" -ex "info proc mappings" \
        --args ./peload/build/peload "<plug-in>" --params
    # the r-xp anonymous region is the image; rip - region_start + .text RVA
    # is the RVA. Then:
    objdump -D -b binary -m i386:x86-64 -M intel --adjust-vma=<addr> chunk.bin

If the faulting instruction is `call [rip+…]`, that target is an IAT slot: parse
the import table and it names the stub that lied. That is how `__RTDynamicCast`
and `D3D11CreateDevice` were both identified.

Ghidra is set up here (`/opt/ghidra`, `scripts/DecompileAll.java`,
`DumpRttiClasses.java`). `analyzeHeadless … -process <prog> -noanalysis` reuses
an analysed project, which makes a second question cheap.

## Known-imperfect, deliberately

  * **GDI+ strokes** are a filled quad per segment; joins are not mitred. At the
    widths a panel outline uses the difference is invisible, and a proper join
    needs the whole outline offset.
  * **GDI+ gradients** interpolate along their axis only; a preset blend keeps
    its end colours and drops the intermediate stops. A path gradient is
    approximated by centre and first surround colour.
  * **`GdipAddPathString`** adds the text's box, not its outlines. Bounds and
    hit-testing stay right where drawing it would be wrong.
  * **Images** scale nearest-neighbour, and only the alpha row of a colour
    matrix is honoured — which is what a plug-in uses one for here, fading a
    disabled control.
  * **`WaitForMultipleObjects`** polls at 1 ms rather than waiting on a shared
    condition variable. It gets the answers right, which is the part callers
    branch on; the handle table is not built for the alternative.
  * **Dash patterns** are accepted and ignored. A dashed outline drawn solid is
    cosmetic; refusing the call is a control that does not appear.
  * **WIC** decodes only. No encoding, metadata, palettes or colour management.
  * **Direct3D** is a walk, not an implementation. Anything that would touch
    hardware answers no; the swap chain presents by copying the plug-in's
    window into the one the host reads, because there is no compositor here to
    walk child windows and this is the only child that draws.

## Two subsystems added since

### GDI, for plug-ins that draw with it rather than blitting

`win32gui.h` was written for plug-ins that rasterise their own interface and
blit the result -- iPlug2 with Skia. A GDI-era editor asks GDI to draw, and most
of what it asks for was a no-op returning success: `SetTextColor`, `MoveToEx`,
`LineTo`, `Ellipse` and `DrawText` discarded their arguments, pens and brushes
were created and never selected, `CreatePen` threw its colour away, and
`CreateFontIndirect` stored no `LOGFONT` at all.

Real now, and general to any plug-in of that vintage: pen/brush/font selection
and the DC state behind it, `Rectangle`, `PatBlt`, `SetPixel`, `GetPixel`,
`Polyline`, and `TextOut`/`ExtTextOut`/`DrawText`/`GetTextExtentPoint`/
`GetTextMetrics` rasterised through the FreeType face DirectWrite already owned.
Text had been measured at a flat seven pixels per character.

Three were wrong rather than absent, and each is the kind that survives review:

  * **COLORREF is `0x00BBGGRR`** and these surfaces are `0x00RRGGBB`. `FillRect`
    wrote one into the other, so every GDI fill had red and blue swapped --
    invisible while the only fills were grey.
  * **`GetStockObject` returned a brush for every index**, so selecting
    `BLACK_PEN` set the DC's brush and a plug-in outlining a shape filled it.
  * **`RectVisible` did not exist.** It cannot fail, so nobody checks it, so a
    stub answering 0 says "none of your interface is visible" and a well-written
    plug-in draws none of it.

`CreateDIBSection` is new and is how a plug-in gets at its own pixels; 32bpp
top-down aliases our surface, and the other layouts convert in whichever
direction is newer. Syncing unconditionally gets that exactly wrong -- it copies
the untouched buffer over GDI's drawing, so each call erases the one before.

**The heap now knows its own pointers.** A plug-in frees things it never got
from us and frees things twice, and that went straight to glibc: a wild pointer
took the host down, a plausible one corrupted the heap quietly. The quiet one
cost a long chase -- an MFC editor faulting inside its own
`CMapPtrToPtr::Lookup` on a bucket array full of small integers. A header in
front of each block was the first attempt and is wrong, because validating a
foreign pointer then reads the bytes before it; ASan said so on the first run,
on a `FormatMessage` buffer that was `malloc`'d and `LocalFree`'d, which is
correct on Windows where malloc *is* HeapAlloc. The table is to one side now,
and every stub handing back a buffer the caller frees allocates through it.

**ASan is worth knowing about here.** 32-bit works:

    cc -m32 -fsanitize=address -O1 -g -I peload -o /tmp/p32 peload/pe32.c \
       -lpthread -lm -L/usr/lib32 -lfreetype /usr/lib32/libX11.so \
       -lpipewire-0.3 -lasound

It found the first corruption in one run, and it makes a layout-sensitive
overrun survivable, which is how the i386 editor was made to complete at all.

### The MSVC C++ library, so its DLL is not needed at all

`msvcp_shim.h`. Every layout in it was read out of the real DLL, and the
derivation is recorded beside the code -- `tools/msvcp_dis.py` is how: name an
export and it disassembles it.

    tools/msvcp_dis.py runtime/msvcp120.dll '?exceptions@ios_base@std@@QEAAXH@Z'
    tools/msvcp_dis.py runtime/msvcp120.dll --grep _Getcat
    tools/msvcp_dis.py runtime/msvcp120.dll --rva 0x3ccc4 --bytes 200

The three plug-ins that needed `msvcp120.dll` -- NI Absynth 5, FM8 and
Kontakt 5 -- now render **byte-identical audio with the DLL removed**. That is
the check to repeat: render once with `runtime/msvcp120.dll` in place, move it
aside, render again with `PELOAD_MSVCP_STRICT=1`, and `cmp` the two WAVs.
Strict mode aborts on the first msvcp symbol that is not implemented, which is
what turns "it still works" into "it never reached the real one".

What that took, in the order the plug-ins hit it:

**The threading primitives had the wrong indirection, and it deadlocked.**
In VC12 `_Mtx_t` is an opaque `void *` and every entry point takes its
*address*: `_Mtx_init` calls `calloc(1, 0x48)` and writes the object through
its argument, and `_Mtx_lock`, `_Mtx_unlock`, `_Mtx_current_owns`, `_Mtx_destroy`
and `do_lock` all open with `mov rax, [rcx]`. Ours locked the caller's pointer
slot instead of the mutex, so the one thread blocked on a lock nothing held --
a single-threaded `futex_do_wait` with no CPU time, reached with nothing
unimplemented. From VS2015 the mutex lives in the caller's storage and the same
exported names take the object directly. Both spellings arrive under one
registration, so the objects carry a magic in their first eight bytes and say
which they are, rather than the shim guessing at a runtime version.

The field layout is msvcp120's own: type at `+0x00`, the lock at `+0x08` where
it keeps a CRITICAL_SECTION, the owning thread at `+0x40`, the recursion count
at `+0x44`. `do_lock` compares the owner *before* it touches the critical
section and simply counts up when the caller already holds it, so a recursive
pthread mutex is the faithful reading of `_Mtx_plain`, not merely a cautious one.

**`_Pad` is how `std::thread` starts.** Its constructor writes its vftable,
`_Cnd_init`s `+0x08`, `_Mtx_init(+0x10, _Mtx_plain)`, clears `_Started` at
`+0x18` and then *takes* the mutex -- which is only visible from the destructor
unlocking it first. `_Launch` hands `_Thrd_start` an entry that calls vftable
slot 0 (the pure `_Go` the plug-in overrides) and then loops
`while (!_Started) _Cnd_wait(&_Cond, &_Mtx)`; `_Release`, on the new thread,
takes the mutex, sets the flag, signals and drops it. The handshake is the
point: it holds the launching thread inside `_Launch` until the new one has
copied what the `_Pad` was holding for it.

**A guest thread needs its TEB before its first instruction.** `_Thrd_create`
went straight to `pthread_create`, and the first plug-in function with a frame
over a page crashed in `__chkstk`, which reads the stack limit from `gs:0x10`.
Without a TEB there that is a wild pointer it writes zeros through. It now goes
through `teb_install()`, the same contract the Win32 `CreateThread` path uses.

**The facets are built, not left to the plug-in.** `ctype<char>::_Getcat`
allocates `0x30` -- facet(16) plus the 32-byte `_Ctypevec` it copies from
`_Locinfo::_Getctype()` -- and `table()` is `mov rax,[rcx+0x18]; ret`, which
fixes `_Ctypevec::_Table` at `+0x18`. That offset matters more than most:
`ctype<char>::is()` is *not* virtual, so callers index the table themselves and
the pointer has to lead somewhere real. Its vftable has eleven slots, not the
wide facet's fifteen, because `do_is` and `do_scan_is` do not exist for the
narrow one. `codecvt<wchar_t,char,int>` converts as UTF-8: on Windows the facet
follows the ANSI code page, and UTF-8 is what that means on the host these
plug-ins actually run on -- identical for the ASCII all of them put through it.

**The stream tier is real formatting, not a sink.** `ios_base` accessors,
`basic_ios`, the wide `basic_streambuf` (shared with the narrow one except where
an element is two bytes), and the whole inserter and extractor set for both
widths: every integer width, both floats, `bool`, `const void *`, the
manipulators, `put`/`write`/`flush`/`read`/`seekg`/`tellg`. Padding honours
`width`, `fill` and the adjust field; the base, sign, showbase, precision and
float-format flags all do what they say. `cout`, `cerr`, `clog` and `cin` are
constructed objects over a streambuf that writes to the matching stdio stream,
because they are imported as *data* -- the plug-in gets their address at load
time and there is no later chance to build them.

The one boundary, stated plainly: the inserters format directly and write
through the streambuf's own `xsputn` virtual rather than going through
`use_facet<num_put<E>>`. A program that installed a `num_put` of its own would
not get it consulted. Nothing in the corpus does -- none of them so much as
import `num_put` -- and the streambuf dispatch, which is the part a plug-in's
own `basic_streambuf` override can observe, is faithful.

The classic locale's facets are still not built eagerly. `use_facet` is a
template inlined into the plug-in, so when the locale does not have a facet the
plug-in's own code constructs it -- an empty vector is a working locale.

A real msvcp is still preferred wherever installed, so nothing that works today
changes; the built-in implementation is what runs when there is none.

## Getting the corpus to 115 of 115

Everything in the corpus loads now, at both widths, and every editor draws. The
work was almost entirely in Win32 rather than in the loader, and `tools/stub_audit.py`
is what found most of it: it walks every binary in a corpus, works out which
imports this host only stubs, and ranks them by how many plug-ins want each one.

    tools/stub_audit.py /storage01/synth_stuff/vst/windows

A stub is not a neutral answer. It returns a failure value, and the caller either
takes its error path -- which may be "give up" -- or reads the zero as success
and carries on with nothing. Both happened here, repeatedly.

### The environment did not exist

`getenv` and `_wgetenv` answered NULL for every name. NI Massive calls
`_wgetenv(L"CommonProgramFiles(x86)")`, asserts when it comes back empty and
calls `ExitProcess(1)` from inside MSVC's `abort()` -- the plug-in simply never
loaded, and the log said nothing about why. Finding it meant reading the
assertion text out of the `MessageBoxW` it was about to show
(`massiveinstrument.cpp:368`), then locating the check by searching the image for
the code that pushes that file name and the line number.

`w32_env_build` now answers with the same `~/.peload` prefix the shell folders
use, creating each directory as it hands the path out, so a path built from
`%APPDATA%` and one from `SHGetFolderPath` land in the same place. The host's own
environment is deliberately not exposed -- a plug-in reading `PATH` expects
Windows paths. `GetEnvironmentVariable`, `SetEnvironmentVariable`,
`ExpandEnvironmentStrings`, `_putenv`, `getenv_s` and `_dupenv_s` all come from
the same table.

### i386 C++ member functions are __thiscall

This one took all four 32-bit NI plug-ins down inside a static initialiser.
`MS` is stdcall on i386, and the C++ member entry points -- `??0exception@std@@QAE@ABQBDH@Z`
and the rest -- were declared with it. A `__thiscall` member takes `this` in ECX
and only the remaining arguments on the stack, so every argument was shifted
along by one: `exception(char const *const &, int)` read the `int` as the message
pointer and dereferenced 1. `MSTHIS` is the convention now, and gcc supports
`__attribute__((thiscall))` on i386 directly.

### The Concurrency runtime wants a lot before it will start

With the argument shift fixed, the same four then threw
`Concurrency::scheduler_resource_allocation_error` out of ConcRT's own
initialisation. That exception carries no message, so each round meant finding
the throw site in `msvcr120.dll` -- `tools/msvcp_dis.py --rva`, then searching
for whatever jumps into that block -- and reading what it had just called. In
order, it wanted:

  * **The processor topology.** `GetProcessAffinityMask`, `GetActiveProcessorCount`,
    `GetNumaHighestNodeNumber` and the rest were not implemented at all, so they
    reported failure.
  * **The processor *group* API.** ConcRT resolves `SetThreadGroupAffinity` and
    `GetThreadGroupAffinity` through `GetProcAddress` and throws when they are
    absent rather than falling back, so `GetLogicalProcessorInformationEx` and
    the group calls are implemented too: one group holding every processor,
    which is the truthful answer for any machine with 64 or fewer cores.
  * **combase.dll to be loadable.** It opens it with
    `LoadLibraryExW(..., LOAD_LIBRARY_SEARCH_SYSTEM32)` and throws on NULL. A
    list of stock Windows DLLs now loads successfully -- `GetProcAddress` against
    one still answers NULL for anything unimplemented, which is what a caller
    probing for an optional export expects. The graphics DLLs are deliberately
    left out: a plug-in that finds d3d11 present takes its Direct3D path.
  * **RoInitialize and RoUninitialize**, which it requires even in a plain
    desktop process.
  * **RegisterTraceGuidsW.** The CRT stores the pointer XOR'd with a cookie of
    its own and compares the slot against `EncodePointer(NULL)` -- a comparison
    that cannot match, because the two encodings are unrelated. On Windows that
    is harmless, since advapi32 always has the function; here NULL decoded to a
    call through address zero.
  * **RegisterWaitForSingleObject**, and then the whole Vista thread pool:
    `CreateThreadpoolTimer`, `SetThreadpoolTimer`, `CreateThreadpoolWait`,
    `CreateThreadpoolWork` and their Close/WaitFor pairs. One thread per object
    rather than a shared pool, but the contract callers depend on is kept.
  * **GetThreadTimes**, which it uses to measure how much work a virtual
    processor is getting through.

### i386 threads ran out of segment selectors

With a scheduler running, the new threads reported
`set_thread_area: No such process`. i386 has three GDT slots for thread-local
bases and glibc has one; `teb_install` is now idempotent per thread, and when the
GDT is full it falls back to the LDT, which has 8192 entries for the process --
one selector per thread, allocated once and never reused, since two threads
sharing a selector would share a TEB.

### Two halves of a pair, from different runtimes

`longjmp` was ours and `setjmp` was the real CRT's: the MSVC compiler emits
`_setjmp3` on i386, that was not in the table, and our `longjmp` found no magic
in the buffer and refused to jump. Registering `_setjmp3` against the same
implementation is all it needed.

And on i386 `_CxxThrowException` aborted with "dispatch is not implemented on
this architecture" -- but this host *does* dispatch i386 C++ exceptions, through
the `fs:[0]` chain, whenever they arrive as `RaiseException`. It now raises the
three parameters that make an exception record a C++ one, exactly as MSVC's own
does. That is what stopped the i386 NI editors from opening.

### File mapping, and the bug underneath it

Every binary in the corpus imports `CreateFileMapping` and `MapViewOfFile`. They
are mmap either way, so implementing them properly was less work than pretending
-- but doing so exposed something else. Kontakt opens its database with the CRT
and then calls `_get_osfhandle` to get a Win32 handle for it. That was a stub
answering zero, so `CreateFileMapping` took the anonymous path and Kontakt went
on to parse a zeroed page as its database -- and the 7-bit length prefix at the
front of it, being all zeros, never terminated. `_get_osfhandle` and
`_open_osfhandle` now bridge the two, and the handle is borrowed exactly as on
Windows: `CloseHandle` leaves the descriptor alone.

Sections with no file behind them are backed by a `memfd` rather than
`MAP_ANONYMOUS`, because two views of one section have to see the same bytes.
And a fresh section reports no error at all: callers tell "I made this, so I
must fill it" from "it was already there" by asking `GetLastError` for
`ERROR_ALREADY_EXISTS`.

### The rest of the audit

`GetFileSizeEx` and `SetFilePointerEx`; `FindFirstFileEx`, which is what a modern
CRT calls and whose absence meant a plug-in enumerating its own preset directory
found nothing; `GetSystemDirectoryW`; the "Ex" locale calls, which take a locale
name rather than an LCID; `GetDateFormat` and `GetTimeFormat`; `PeekNamedPipe`
answering "nothing waiting" rather than "the call failed".

The file, folder and colour choosers are real, through zenity, falling back to
Cancel with no display. `ShellExecute` opens documents and links with `xdg-open`.

wininet is answered as a machine with no network, deliberately: a plug-in
reaching for the internet is checking for updates or calling home, and a host has
no business doing either on its behalf. What it does owe them is a coherent
answer, and the stub's zeros were not one -- a NULL handle from `InternetOpen`
followed by a "success" from `InternetReadFile` is a state nobody has a path for.
Offline is a state every one of them handles, because it happens on real machines.

### The mouse went to the wrong window

Editors painted and ignored every click. Windows hands the mouse to the *deepest
child window under the pointer*, in that child's own client coordinates, and
`w32_mouse` was delivering everything to the plug-in's top-level window instead.

That is fine for an editor drawn into one window -- most of the corpus -- and
useless for one built as a wrapper around a panel. MinimogueVA is the second
kind: `VSTGUI` at #2 wrapping `GMPIGUI` at #3, which is where the WndProc that
handles input lives. Every click landed on the wrapper. `WindowFromPoint` was a
reached stub answering zero as well, so a plug-in hit-testing its own panel
concluded the pointer was over nothing.

`w32_child_at` now descends from the root to the deepest visible child
containing the point, translating coordinates as it goes and remembering the
last window that has a WndProc -- a container with none cannot be a target,
because the message would go nowhere rather than to the parent that would have
handled it. A held capture still takes precedence, with the coordinates
translated into the capture window's client space.
`WindowFromPoint`, `ChildWindowFromPoint` and `ChildWindowFromPointEx` answer
from the same walk.

To check a control by hand, `--drag` reports which parameter moved:

    peload MinimogueVA.vst3 --editor out.ppm --drag 644,78,644,38
      dragged 644,78 -> 644,38, 16 pointer step(s) delivered -- Cutoff 0.345 -> 0.545

Aim at an actual knob. A grid of arbitrary points mostly lands between them and
reports "nothing moved", which looks exactly like the bug this replaced --
capture the editor to a PNG and pick coordinates off it instead.

### Adjusting one knob wiped the rest of the panel

Direct2D's clip was recorded and then ignored: `PushAxisAlignedClip` stored the
rectangle in a global and no drawing call ever read it.

That is invisible on a first paint and ruinous on the second. A plug-in
repainting one knob pushes that knob's rectangle, redraws its whole background
bitmap under it, and then draws only the controls the rectangle touches --
everything else is still on the window from the last frame, which is how
Windows works. With the clip ignored, that background bitmap covered the entire
editor, so moving one knob left the bare backdrop and a single control behind.

The clip is a stack of device-space rectangles now, nested by intersection, and
`d2_gfx` applies the one in force to every graphics object it builds -- it has
to be applied there, because that object is rebuilt for every call. `Clear`
fills the clip rather than the target, which is what Direct2D's does, and
`BeginDraw` empties the stack so an unbalanced push cannot clip the next frame
away to nothing.

Worth measuring rather than eyeballing: capture the editor, drag a knob,
capture again, and diff. One knob should be the only thing that changed.

    before the fix:  97805 pixels changed, bounding box (24, 35, 975, 570)
    after:            1589 pixels changed, bounding box (626, 63, 673, 108)

### What is not this host's problem

Three plug-ins load and render silence, and all three are the plug-in's own
doing rather than a defect here:

  * **brokenmini** starts with its Power switch off and its Fuse blown -- it
    emulates a broken Minimoog. Set `Power` and `Voltage` to 1 and `Fuse` to 0
    in a saved patch and it renders at peak 0.4490.
  * **drumtraqs** is a drum machine. The sweep plays note 60, which is an empty
    pad; notes 36, 38 and 42 give 0.2018, 0.1954 and 0.1238.
  * **NI Kontakt 5** is a sampler with no instrument loaded.

**NI Massive needs its factory data installed**, which is a setup step and not a
code change: `tables.dat` from the Massive installer goes in
`~/.peload/Program Files (x86)/Common Files/Native Instruments/Massive/` for the
64-bit build and `~/.peload/AppData/Roaming/Native Instruments/Massive/` for the
32-bit one. Without it `VSTPluginMain` returns no AEffect, and the log says so.

## The tree as you find it

`main` is a long way ahead of `origin/main`, which is still on a09389c.
Nothing in this document is pushed; `git rev-list --count origin/main..main`
gives the number, and stays right after the next commit in a way a number
written here does not. The version in the tree is
**0.2.0**, in all four places `version-consistent` checks — origin's 0.1.1 is
the last thing released, not the last thing built.

`release-notes.txt` is untracked and stale. It describes 0.1.1 and the GitHub
Release that was never created for it; read it for the publishing steps, which
still apply, and not for what this tree contains.

`.claude/worktrees/ni-loader-fixes` is **dead**. The directory is gitignored as
of 95e7590, and its branch `worktree-ni-loader-fixes` is a single docs-only
commit whose content is superseded by this file. It is not work in progress and
there is nothing to merge out of it — a reader who finds the worktree and
assumes otherwise will waste an afternoon on it.

### Files this session rewrote

Expect conflicts here and nowhere much else:

    peload/winstubs.h       peload/pehost.c      peload/win32gui.h
    peload/dwrite_shim.h    peload/vst3.c
    peload/gdiplus_shim.h   (new)
    peload/d3d_shim.h       (new)

### Before you commit

    python3 tools/regress.py --no-build

18 of 20 pass and 2 skip. That is the baseline; anything under it is yours.

`sources-tracked` caught a header that was never `git add`ed twice in this
session — both new shims arrived that way, because a new file is the one kind of
change `git commit -a` cannot see. If that check fails, that is what it is
telling you, and the answer is a `git add`, not an edit to the check.

## Outstanding

  1. **The corpus is complete.** 115 of 115 load -- 40 VST2-64, 35 VST3, 40
     VST2-32 -- and every editor draws at both widths. The three that render
     silence do so because of their own default patch, their own note map, or an
     empty sampler; see the section above.

  2. **Themed controls.** The built-in classes -- BUTTON, STATIC, EDIT,
     COMBOBOX, LISTBOX, SCROLLBAR -- exist and draw the classic look. A plug-in
     that expects the XP-and-later themed look gets the pre-XP one, which is a
     difference in appearance rather than in function; the artwork for the other
     lives in a .msstyles file this host has no business shipping.

  3. **msvcp140, and the i386 half of msvcp_shim.h.** msvcp120 is done: every
     symbol the corpus imports is implemented and the four NI plug-ins render
     byte-identical audio without the DLL. The VS2015 spellings are registered
     against the same implementations wherever the name differs only in the
     state type.

     `MinimogueVA.vst3` (msvcp140, outside the corpus) is the one to pick up.
     Without `runtime/msvcp140.dll` it loads, creates its `IEditController` and
     reads its `parameters.se.xml`, then segfaults at its own RVA 0xa093f --
     `mov rcx,[rdi+0x18]; mov rax,[rcx]; call [rax+0x10]`, an inlined `_Decref`
     against an object in our heap whose vftable has already gone. So it is a
     facet or `_Locimp` lifetime, one release too many somewhere. One such bug
     is already fixed -- `basic_streambuf::getloc` returns the locale by value
     and was not taking the reference the caller's destructor drops -- but that
     was not this one.

     `msvcp_shim.h` is still inside `#if defined(__x86_64__)`. The i386 entry
     points need `__thiscall`, which `MSTHIS` now provides.

  4. **The 0.2.0 `.rpm`** has not been built. The spec is bumped and has a
     changelog entry; it needs the silverblue43 VM (see `release-notes.txt`).

  5. Everything above sits unpushed on `main`.

Not worth doing: a software Direct3D 11 that actually rasterises. The walk above
is the right shape and it already gets to the drawing calls.
